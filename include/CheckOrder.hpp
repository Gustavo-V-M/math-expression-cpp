#ifndef CHECKORDER_H
#define CHECKORDER_H

#include <string>
using namespace std;

bool isCorrectOrder(string expression);
bool isOpening(char current);
char isClosing(char current);
#endif // CHECKORDER_H
